# Security Notes

This is a public showcase repository.

- No environment files (`.env`) are included.
- No secrets, tokens, or passwords are included.
- No production database dumps are included.
- Admin access is JWT-protected in production.
- Production configuration uses host allow-lists and HTTPS.

If needed, private code/demo access can be shared separately.
